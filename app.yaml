# configuration file for GAE
# 

runtime: nodejs
vm: true
threadsafe: true
service: kvasir

health_check:
    enable_health_check: false

handlers:
 - url: /.*
   script: server.js
   secure: always
   login: admin
